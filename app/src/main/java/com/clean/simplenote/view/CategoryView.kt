package com.clean.simplenote.view

import android.content.Context
import android.util.AttributeSet
import androidx.constraintlayout.widget.ConstraintLayout

class CategoryView(context: Context, attrs: AttributeSet?, defStyleAttr: Int) :
    ConstraintLayout(context, attrs, defStyleAttr) {

    /*fun setSelectedCategory() {
        if (isSelectedCategory()) {
            // TODO: 선택된 카테고리인지 확인하는 협력을 통해 선택된 것이면 텍스트를 볼드 처리한다.
            // 협력의 상대는 MainActivity가 될 듯
        }
    }

    private fun isSelectedCategory(): Boolean {

    }*/
}